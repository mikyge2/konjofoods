# Backend

This directory contains the Python backend server.

## Getting Started

To get started, install the dependencies:

```bash
pip install -r requirements.txt
```

Then, start the backend server:

```bash
python server.py
```
